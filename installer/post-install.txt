                    GrimLedger installed successfully


BROWSER BRIDGE — NEXT STEPS
---------------------------
Extension ID (store / dev):  ldehlncibafipkfjhfkihkonmcllhjen

1. Install GrimLedger Bridge from the Chrome Web Store when available,
   OR open Chrome/Edge → chrome://extensions → Developer mode → Load unpacked:
   %LOCALAPPDATA%\GrimLedger\browser-extension

2. Native messaging host: registered during setup if you kept the pre-filled ID.

3. In GrimLedger: Settings → enable "browser bridge" → Save Settings.

4. Unlock GrimLedger and keep it running while using the extension.


NATIVE MESSAGING HOST
---------------------
If you skipped the extension ID during setup, run:
   %LOCALAPPDATA%\GrimLedger\app\native-host\install-windows.ps1 -ExtensionId ldehlncibafipkfjhfkihkonmcllhjen


VAULT DATA
----------
Your vault is stored at:
   %APPDATA%\GrimLedger\vault.grim

Uninstalling the app does not delete your vault unless you remove it manually.
